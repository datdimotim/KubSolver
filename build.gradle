group 'KubSolver'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.7

jar {
    manifest{ manifest.attributes('Main-Class' : 'test.TestsAndBenchmarks'); }
    baseName = 'KubSolver'
    version =  '1.0'
}

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
}

dependencies {
    compile 'com.github.datdimotim:CompactArrays:1.0.0'
}
